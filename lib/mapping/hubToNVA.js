module.exports = {
    // by Thing item name
    // 'basic' : {
    //     nva : {
    //         verb   : 'set',
    //         adverb : ({ item, message }) => {
    //             return  {
    //                 state : item.enum[message.value] === 'thing.device.value.off' ? 'off' : 'on'
    //             };
    //         }
    //     }
    // }
}
